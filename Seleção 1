package main

import "fmt"

func main() {
	var num1, num2 float64
	fmt.Print("qual é o primeiro numero?")
	fmt.Scan(&num1)
	fmt.Print("Qual é o segundo numero?")
	fmt.Scan(&num2)

	if num1 > num2 {
		fmt.Println("primeiro numero é o maior")
	} else {
		fmt.Println("segundo numero é maior")
	}
}
